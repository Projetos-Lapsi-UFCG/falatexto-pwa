services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: assis_frontend
    restart: unless-stopped
    ports:
      - "4200:80"
    networks:
      - falatexto-network

networks:
  falatexto-network:
    driver: bridge
